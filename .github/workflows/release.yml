name: Build & Release (.NET 8)

env:
    CONFIGURATION: Release
    DOTNET_VERSION: 8.0.x

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore CsSpawnsPlugin/CsSpawnsPlugin.csproj

      - name: Build (Release)
        run: |
          dotnet build CsSpawnsPlugin/CsSpawnsPlugin.csproj \
            --configuration Release \
            --no-restore

      - name: Collect DLL
        run: |
          mkdir artifact
          cp CsSpawnsPlugin/bin/Release/net8.0/CsSpawnsPlugin.dll artifact/
          cp CsSpawnsPlugin/bin/Release/net8.0/CsSpawnsPlugin.pdb artifact/ || true

      - name: Zip release artifact
        run: |
          cd artifact
          zip -r ../CsSpawnsPlugin-${{ github.ref_name }}.zip .

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: CsSpawnsPlugin-${{ github.ref_name }}.zip